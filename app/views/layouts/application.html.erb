<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title><%= content_for(:title) || "Reading Room" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-50 text-gray-900">
    <!-- HEADER -->
    <header class="bg-white border-b border-gray-200">
      <div class="max-w-6xl mx-auto px-3 sm:px-4 lg:px-6 py-4 flex items-center justify-between">
        <%= link_to "📚 Reading Room", books_path, class: "text-xl font-semibold text-gray-900 hover:text-gray-700" %>

        <nav class="flex items-center gap-4 text-sm">
          <% if current_user %>
            <span class="text-gray-600">
              Olá, <strong><%= current_user.name %></strong>
            </span>

            <%= link_to "Cadastrar livro",
                        new_book_path,
                        class: "px-3 py-1.5 rounded-md bg-primary text-white hover:bg-primary/80 transition" %>

            <%= button_to "Logout",
                          session_path,
                          method: :delete,
                          class: "px-3 py-1.5 rounded-md bg-secondary hover:bg-secondary/80 transition",
                          form: { class: "inline" } %>
          <% else %>
            <%= link_to "Criar conta",
                        new_user_path,
                        class: "px-3 py-1.5 rounded-md bg-primary text-white hover:bg-primary/80 transition" %>

            <%= link_to "Login",
                        new_session_path,
                        class: "px-3 py-1.5 rounded-md bg-secondary hover:bg-secondary/80 transition" %>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- FLASH MESSAGES -->
    <div class="max-w-6xl mx-auto px-3 sm:px-4 lg:px-6 mt-4 space-y-2">
      <% if notice %>
        <div class="rounded-md bg-green-50 border border-green-200 text-green-800 px-4 py-3">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="rounded-md bg-red-50 border border-red-200 text-red-800 px-4 py-3">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <!-- MODAL SLOT -->
    <turbo-frame id="modal"></turbo-frame>

    <!-- MAIN CONTENT -->
    <main class="max-w-6xl mx-auto px-3 sm:px-4 lg:px-6 py-6 sm:py-8">
      <%= yield %>
    </main>
  </body>
</html>
